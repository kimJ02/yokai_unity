# 요괴전선 Unity 포팅 — 전투 코어 프로토타입 스펙

> **최종 목표: `reference/project_test.html` 원본을 Unity로 그대로(4캐릭터·가챠·아이템·9지역·윤회 전부) 포팅하는 것.**
> 한 번에 다 만들지 않고 **핵심 루프부터 순서대로** 쌓아 올린다. 이 문서는 그 1단계(전투 코어)만 다룬다 — 아래 "범위 밖" 항목은 빠지는 게 아니라 다음 스프린트에서 순서대로 포팅될 예정이다.

이 문서 하나 + 이 레포를 클론하면 바로 작업을 시작할 수 있도록, 1단계의 범위와 수치를 확정해뒀다.

## 이번 스프린트에서 만드는 것

- 몬스터 스폰 알고리즘 + 필드 구성
- 공격 버튼 1개
- 전투 판정만 (연출·사운드·UI 없음)
- 캐릭터·몹은 전부 **원(circle)** — 아군/적군은 **색만** 다름, 크기 동일
- 이동은 **기존에 구현된 캐릭터 컨트롤러를 그대로 가져다 씀** (직접 새로 안 만든다)

## 이번 스프린트 범위 밖 — 이번엔 Unity로 포팅하지 않는다

캐릭터 4종 구분, 스킬트리/전문화, 아이템 35종, 가챠, 지역 9종, 윤회(프레스티지), 세이브/로드, HUD/UI 전반.

> 이 시스템들은 **원본(JS/HTML5) 게임에는 이미 전부 완성돼서 존재한다.** 없어진 게 아니다.
> Unity는 C#으로 처음부터 새로 짜는 별개의 프로젝트라 원본 기능이 자동으로 따라오지 않을 뿐이고,
> 이번 스프린트는 그중 전투 코어 하나만 먼저 Unity로 옮기는 단계라는 뜻이다.
>
> `reference/project_test.html`은 그 완성된 원본 게임 전체 파일이다(수치·로직 **참고용**).
> 열어보고 값은 가져다 써도 되지만, 이 문서에 없는 시스템(가챠 등)을 Unity에 같이 만들지 말 것 — 나중 스프린트에서 순서대로 포팅한다.

---

## 1. 필드 구성

**결정: 원본과 동일하게 횡스크롤 구조.** ~~중력·점프 없는 평면 아레나~~ ← 이전 버전의 오판. 원본은 X(좌우)만 자유 이동이고 Y(높이)는 고정 바닥 + 점프 시에만 중력을 받는 사이드뷰 플랫포머다. 다만 **여러 층 발판은 이번 스프린트에서 제외**(바닥 1개만) — 전투 판정에 집중하기 위해서고, 발판/다층 구조는 필요해지면 나중 스프린트에서 붙인다.

- X축 경계가 있는 필드, 단일 고정 바닥(Y). 캐릭터 이동속도로 화면을 3~4초 안에 가로지르는 정도의 폭으로 잡으면 원본 체감과 비슷하다.
- 원본 상수를 100px = 1유닛 기준으로 축척: 이동속도 270px/s → 2.7 · 점프속도 960px/s → 9.6 · 중력 2600px/s² → 26 (원본 Y+는 화면 아래라 부호 반전 주의).
- 캐릭터는 필드 X 경계 밖으로 못 나가게 clamp. Y는 자유 이동이 아니라 점프 물리로만 바뀌고 바닥에서 멈춘다.
- 카메라는 고정(Orthographic, 필드 폭 전체가 한 화면에 들어오게).

## 2. 몬스터 스폰 알고리즘

원본 로직을 **몹 종류 1개**로 단순화한 것. 숫자는 원본에서 이미 튜닝된 값이니 그대로 시작점으로 쓰고, 플레이해보면서 조정.

```
매 3.6초마다 웨이브 발생:
  최대 7마리까지 스폰 시도
  단, "현재 살아있는 몹 수 + 이번에 스폰할 수" >= 22 면 중단(전체 상한 22)
  각 마리마다:
    필드 안에서 무작위 위치 하나 뽑기
    이미 배치된 몹과 너무 가까우면(최소 간격 미달) 다시 뽑기 — 최대 10회 재시도
    10회 넘게 실패하면 이번 마리는 스킵하고 다음으로
```

- 몹 1종 스탯(원본 "오니" 기준, 시작점): 체력 38 · 공격력 13 · 이동속도 76
- 몹은 가장 가까운 아군 쪽으로 단순 직선 이동 + 접촉 시 공격

## 3. 공격 (버튼 1개)

**결정: 근접 판정형.** 버튼을 누르면 캐릭터 반경 R 안의 적 전체에게 즉시 피해, 쿨다운 후 재사용. (투사체 오브젝트 없음 — 가장 단순한 형태로 시작)

- 시작 수치: 공격력 10 · 판정 반경 R(캐릭터 크기의 2~3배 정도로 시작) · 쿨다운 0.4초
- 데미지 처리는 최대한 단순하게: **몹 체력을 hp 1로 두고 맞으면 즉시 사망**으로 v0을 먼저 완성한 뒤, 체력바 개념은 그다음에 붙여도 된다.

### 조작키 — 원본(`KEYMAP`, project_test.html) 그대로 따른다, 임의로 바꾸지 말 것

| 키 | 동작 |
|---|---|
| ← / → (ArrowLeft/Right) | 좌우 이동 |
| **C만** (Space 아님) | 점프 |
| Z | 공격(이번 스프린트의 버튼 1개) |
| X | 스킬(원본은 `ult` — 이번 스프린트 범위 밖, 나중에) |

WASD나 마우스 클릭 등 원본에 없는 키를 임의로 추가하지 않는다.

> **의도적 예외 — 점프는 C만.** 원본 `KEYMAP`은 `KeyC: 'jump', Space: 'jump'`로 둘 다 점프에 매핑돼 있다(사실이고 확인됨). 하지만 사용자가 명시적으로 "Space는 빼고 C만"으로 지시해서 이번 프로토타입은 C 전용이다. 이건 실수나 미반영이 아니라 **의도된 원본과의 차이**이니, "원본이랑 다른데?"라며 Space를 다시 추가하지 말 것.

## 4. 비주얼 규칙

- SpriteRenderer + 원형 스프라이트(유니티 기본 Circle 스프라이트로 충분)
- 아군 색 / 적군 색 2가지만 존재, 크기는 동일
- 애니메이션 없음, 이펙트 없음

## 5. 이동

기존에 구현된 캐릭터 컨트롤러를 재사용한다. 갖고 있는 게 없다면 **Unity Starter Assets (2D)**(Unity 공식, 무료, Asset Store/GitHub 배포)로 시작할 것을 추천. 다른 걸 이미 쓰기로 했다면 그걸 그대로 사용.

## 6. 저장소 / 환경

- 레포: https://github.com/kimJ02/yokai_unity (Public)
- Unity: 2D (URP) 템플릿
- 클론 후 Unity로 열면 `Library/`가 자동 재생성된다(처음 한 번 시간 걸림, 정상). `.meta` 파일은 전부 커밋돼 있으니 별도 조치 불필요.
- 커밋 시 주의: `Library/ Temp/ Logs/ UserSettings/`는 `.gitignore`에 이미 걸려있어 커밋되지 않는다 — 그 폴더들을 강제로 add 하지 말 것.

## 개발 순서 제안

1. 필드 경계 + 고정 카메라
2. 캐릭터 컨트롤러 임포트 → 스프라이트를 원으로 교체, 색 적용
3. 몬스터 스폰 로직(2번) 구현, 몹도 원으로 표시
4. 공격 판정(3번) 연결
5. 여기까지 되면 "핵심 루프" 완성 — 이후 확장(체력바, 몹 종류 추가 등)은 따로 논의
