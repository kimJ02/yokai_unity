# 요괴전선 Unity 포팅 — 전투 코어 프로토타입 스펙

> **최종 목표: `reference/project_test.html` 원본을 Unity로 그대로(4캐릭터·가챠·아이템·9지역·윤회 전부) 포팅하는 것.**
> 한 번에 다 만들지 않고 **핵심 루프부터 순서대로** 쌓아 올린다. 이 문서는 그 1단계(전투 코어)만 다룬다 — 아래 "범위 밖" 항목은 빠지는 게 아니라 다음 스프린트에서 순서대로 포팅될 예정이다.

이 문서 하나 + 이 레포를 클론하면 바로 작업을 시작할 수 있도록, 1단계의 범위와 수치를 확정해뒀다.

## 이번 스프린트에서 만드는 것

- 몬스터 스폰 알고리즘 + 필드 구성
- 공격 버튼 1개(플레이어 쪽은 마법사 차지샷으로 구체화됨, 아래 3번 참고)
- 전투 판정만 (연출·사운드·UI 없음)
- 캐릭터·몹은 전부 **원(circle)** — 아군/적군은 **색만** 다름, 크기 동일
- 이동은 **기존에 구현된 캐릭터 컨트롤러를 그대로 가져다 씀** (직접 새로 안 만든다)
- **실제 Physics2D 기반 중력/충돌 + 원본 발판(NORMAL_PLATFORMS) 배치** (아래 1번 개정 참고 — 처음엔 "바닥 1개만"이었으나 사용자 요청으로 확장됨)

## 이번 스프린트 범위 밖 — 이번엔 Unity로 포팅하지 않는다

캐릭터 4종 구분, 스킬트리/전문화, 아이템 35종, 가챠, 지역 9종, 윤회(프레스티지), 세이브/로드, HUD/UI 전반.

> 이 시스템들은 **원본(JS/HTML5) 게임에는 이미 전부 완성돼서 존재한다.** 없어진 게 아니다.
> Unity는 C#으로 처음부터 새로 짜는 별개의 프로젝트라 원본 기능이 자동으로 따라오지 않을 뿐이고,
> 이번 스프린트는 그중 전투 코어 하나만 먼저 Unity로 옮기는 단계라는 뜻이다.
>
> `reference/project_test.html`은 그 완성된 원본 게임 전체 파일이다(수치·로직 **참고용**).
> 열어보고 값은 가져다 써도 되지만, 이 문서에 없는 시스템(가챠 등)을 Unity에 같이 만들지 말 것 — 나중 스프린트에서 순서대로 포팅한다.

---

## 1. 필드 구성

**결정: 원본과 동일하게 횡스크롤 구조.** 원본은 X(좌우)만 자유 이동이고 Y(높이)는 고정 바닥 + 점프 시에만 중력을 받는 사이드뷰 플랫포머다.

- **개정(2026-08-25): "바닥 1개만"이던 축소 결정을 철회하고 원본 발판(NORMAL_PLATFORMS) 15개 + Physics2D 실제 충돌로 확장.** 사용자가 "물리엔진이랑 플랫폼도 구현해줘"라고 명시적으로 요청. `CharacterMover2D`가 손으로 계산하던 중력(vy 수동 적분)을 걷어내고 `Rigidbody2D`(동적 바디, 실제 gravityScale) + `Collider2D` 충돌 해석으로 교체했고, 원본 `NORMAL_PLATFORMS` 좌표를 그대로 옮긴 발판을 배치했다(전용 `Ground` 물리 레이어, `BuildPartAScene.cs` 참고).
- **추가 개정(같은 날): 발판을 원본처럼 원웨이(아래/옆 통과, 위에서만 착지)로 수정.** 처음엔 막힌(solid) 콜라이더로 단순화했는데, 이게 원본과 달리 점프 중 발판 밑면에 머리가 막히는 버그였다(사용자가 직접 플레이해보고 지적). `PlatformEffector2D`(`useOneWay=true`) + `Collider2D.usedByEffector=true`로 교체해 해결 — 원본의 `dropTimer`(아래로 뛰어내리기 입력)까지는 재현하지 않음(범위 밖). `PhysicsAndMageTests.Player_PassesThroughOneWayPlatformFromBelow_ThenLandsOnTopFromAbove`로 검증됨.
- **발판 층간 간격도 원본(1.0~1.1유닛)보다 넓힘(1.35유닛)** — 사용자가 "간격이 너무 작다"고 명시적으로 요청한 의도적 편차. 점프 최대 높이(1.772유닛) 대비 76% 지점이라 여유 있게 닿는다. X 배치(각 층의 발판 개수·폭·좌우 위치)는 원본 그대로 유지, Y(층 높이) 값만 조정했다 — 나중에 "원본이랑 다르다"며 1.0~1.1로 되돌리지 말 것.
- 필드 폭은 원본 mapW(2600px) 그대로 100px=1유닛 축척 → `FieldBounds.MinX=0, MaxX=26`. 발판 X 배치(개수·폭·좌우 위치)는 원본 그대로, Y(층 높이)는 위 항목대로 넓혔다(유닛 환산은 `BuildPartAScene.Platforms` 배열 주석 참고).
- 원본 상수를 100px = 1유닛 기준으로 축척: 이동속도 270px/s → 2.7 · 점프속도 960px/s → 9.6 · 중력 2600px/s² → 26 (원본 Y+는 화면 아래라 부호 반전 주의, `Physics2D.gravity`로 전역 설정).
- 캐릭터는 필드 X 경계 밖으로 못 나가게 clamp. Y는 자유 이동이 아니라 점프 물리 + 발판/바닥 충돌로만 바뀐다.
- **카메라 개정**: 필드가 26유닛으로 넓어져 "고정 카메라로 전체를 한 화면에" 두면 캐릭터가 너무 작아진다 — X축만 플레이어를 따라가는 스크롤 카메라(`CameraFollow2D`)로 바꿈. Y는 발판이 전부 한 화면에 들어오는 높이라 고정.
- **카메라 크기 재개정(2026-08-26)**: "맵이 원본보다 좁아 보인다"는 사용자 지적으로 재계산. 원본 캔버스는 1280×720px 고정이라 화면엔 항상 맵(2600px)의 49%만 보이는데, 이전 값(orthographicSize=5.5)은 이 비율과 무관하게 임의로 잡아서 맵의 75%가 한 번에 보였다 — 원본보다 훨씬 넓게 보여서 좁아 보이는 역설이 생김. 100px=1유닛 규칙을 카메라에도 적용: 세로 절반=720px÷100÷2=3.6유닛이 기준값이지만, 발판 층간 간격을 넓힌 우리 필드(1번 항목 참고)에 맞춰 최상단 발판 위 캐릭터가 안 잘리게 `orthographicSize=3.84`로 살짝 키움(하단 여백은 원본 그대로 바닥 아래 1유닛 유지, 상단만 우리 발판 높이 기준 재계산). 가로/세로 비율은 원본 그대로 유지돼 26/12.8≈2.03(원본 2600/1280≈2.03과 일치).

## 2. 몬스터 스폰 알고리즘

원본 로직을 **몹 종류 1개(오니)**로 단순화한 것 — 스폰/이동/충돌 메커니즘 자체는 원본 그대로 이식한다(**개정 2026-08-26: "일단 프로토타입은 대충 굴러가게, 나중에 다듬는다"는 식으로 임의로 단순화하지 않는다** — 사용자가 명시적으로 지적. 이건 실제 구현이지 러프 프로토타입이 아니다). 숫자는 원본에서 이미 튜닝된 값이니 그대로 쓴다.

```
매 3.6초마다 웨이브 발생:
  최대 7마리까지 스폰 시도
  단, "현재 살아있는 몹 수 + 이번에 스폰할 수" >= 22 면 중단(전체 상한 22)
  각 마리마다:
    발판 15개 중심 + 바닥 그리드 6곳, 총 21개의 정해진 스폰 포인트 중 하나를 뽑아
    그 지점 폭 안에서 살짝 흔들기(균등 랜덤 아님) — 발판 포인트는 그 발판의 실제 높이를 씀
    이번 웨이브에서 이미 배치한 몹과 너무 가까우면(X<52px 이면서 Y<12px) 다시 뽑기 — 최대 10회 재시도
    10회 넘게 실패하면 이번 마리는 스킵하고 다음으로(이전 웨이브 몹과는 겹쳐도 됨)
```

- 몹 1종 스탯(원본 "오니" 기준, 시작점): 체력 38(HP 시스템 자체가 범위 밖이라 미사용) · 공격력 13 · 이동속도 76
- **이동 AI(원본 updateEnemies() 오니 분기 그대로)**: 플레이어가 추적범위(300px=3유닛) 안이면 그 방향으로 계속 걷는다 — 도달하거나 멈추는 게 아니라 겹칠 때까지 계속. 범위 밖이면 1.5~3.5초마다 무작위로 방향을 바꾸며 배회한다.
- **중력/착지**: 몹도 플레이어와 동일하게 실제 Physics2D 중력을 받아 발판/바닥 위에 선다.
- **발판 가장자리 반전**: 발판 위(바닥 아님)에 있는 몹은 가장자리 14px 안쪽에서 멈추고 안쪽으로 방향을 뒤집는다 — **원본처럼 발판 위 몹은 걸어서 떨어지지 않는다.**
- **스폰 무적**: 스폰 직후 2초간(`CONFIG.run.spawnProtect`) 모든 공격 판정을 무시한다.
- **접촉 데미지**: 플레이어 Health가 없어(범위 밖) 실제 피해 적용은 스텁 상태 유지.

## 3. 공격 (버튼 1개)

~~결정: 근접 판정형~~ → **개정(2026-08-25): 마법사(원본 무기 `bow`="마법 지팡이") 차지샷으로 구체화.** 사용자가 "마법사 캐릭터 기본공격을 구현해줘"라고 명시적으로 요청해서, 근접 판정 대신 원본 `bowFire()`/`CONFIG.bow` 수치를 그대로 이식한 투사체 공격으로 바뀌었다(`MageAttack.cs` + `MageProjectile.cs`). Z를 누르고 있으면 차지, 떼면 그 시점 차지율로 발사:

| 원본 값(`CONFIG.bow`) | Unity 값 | 의미 |
|---|---|---|
| cd: 0.5 | cooldown 0.5 | 쿨다운(초) |
| dmg: 0.9 | baseDamage 9(=baseAtk10×0.9) | 스탯 시스템 없어 고정값으로 근사 |
| chargeMax: 1.0 | chargeMax 1.0 | 최대 차지 시간(초) |
| chargeDmg: 1.6 | chargeDmgMult 1.6 | dmg = base×(1+1.6×k) |
| pierce: 2, chargePierce: 4 | basePierce 2, chargePierce 4 | pierce = 2+floor(k×4) |
| speed: 1080, range: 880 | projectileSpeed 10.8, projectileRange 8.8 | speed는 (1+0.3×k)배, 수명은 range/speed로 고정(차지와 무관, 원본 그대로) |

- 조준: 원본처럼 ↑/↓ 화살표로 상하 조준 가능(원본 aimY는 화면좌표라 부호 반전해서 Unity Y+ 위 방향과 맞춤). 입력이 없으면 캐릭터가 마지막으로 향한 방향(`CharacterMover2D.Facing`)으로 발사.
- 관통(pierce): 맞은 적마다 즉시 Destroy(v0, Health 없음 — PlayerAttack과 동일 정책), 관통 수를 다 쓰면 투사체도 소멸.
- 스킬트리(fireTier/frostTier)·중력구슬 분기는 범위 밖(이번 스프린트는 버튼 1개, 스킬은 X키=범위 밖).
- **기존 근접형 `PlayerAttack.cs`는 코드에 남겨뒀지만 씬에서 안 씀** — 나중에 다른 무기 캐릭터를 붙일 때 참고용으로 유지.

### 조작키 — 원본(`KEYMAP`, project_test.html) 그대로 따른다, 임의로 바꾸지 말 것

| 키 | 동작 |
|---|---|
| ← / → (ArrowLeft/Right) | 좌우 이동 |
| **C만** (Space 아님) | 점프 |
| Z | 공격(이번 스프린트의 버튼 1개) |
| X | 스킬(원본은 `ult` — 이번 스프린트 범위 밖, 나중에) |

WASD나 마우스 클릭 등 원본에 없는 키를 임의로 추가하지 않는다.

> **의도적 예외 — 점프는 C만.** 원본 `KEYMAP`은 `KeyC: 'jump', Space: 'jump'`로 둘 다 점프에 매핑돼 있다(사실이고 확인됨). 하지만 사용자가 명시적으로 "Space는 빼고 C만"으로 지시해서 이번 프로토타입은 C 전용이다. 이건 실수나 미반영이 아니라 **의도된 원본과의 차이**이니, "원본이랑 다른데?"라며 Space를 다시 추가하지 말 것.

## 4. 비주얼 규칙

- SpriteRenderer + 원형 스프라이트(유니티 기본 Circle 스프라이트로 충분)
- 아군 색 / 적군 색 2가지만 존재, 크기는 동일
- 애니메이션 없음, 이펙트 없음

## 5. 이동

기존에 구현된 캐릭터 컨트롤러를 재사용한다. 갖고 있는 게 없다면 **Unity Starter Assets (2D)**(Unity 공식, 무료, Asset Store/GitHub 배포)로 시작할 것을 추천. 다른 걸 이미 쓰기로 했다면 그걸 그대로 사용.

## 6. 저장소 / 환경

- 레포: https://github.com/kimJ02/yokai_unity (Public)
- Unity: 2D (URP) 템플릿
- 클론 후 Unity로 열면 `Library/`가 자동 재생성된다(처음 한 번 시간 걸림, 정상). `.meta` 파일은 전부 커밋돼 있으니 별도 조치 불필요.
- 커밋 시 주의: `Library/ Temp/ Logs/ UserSettings/`는 `.gitignore`에 이미 걸려있어 커밋되지 않는다 — 그 폴더들을 강제로 add 하지 말 것.

## 개발 순서 제안

1. 필드 경계 + 고정 카메라
2. 캐릭터 컨트롤러 임포트 → 스프라이트를 원으로 교체, 색 적용
3. 몬스터 스폰 로직(2번) 구현, 몹도 원으로 표시
4. 공격 판정(3번) 연결
5. 여기까지 되면 "핵심 루프" 완성 — 이후 확장(체력바, 몹 종류 추가 등)은 따로 논의
