# 스프린트 2 — 체력 & 데미지

> **이전 스프린트 문서**: `docs/sprints/01-combat-core.md` (전투 코어 프로토타입).
> 거기 적힌 **확정 수치와 "의도적 편차"는 지금도 전부 유효하다** — 발판 층간 1.35유닛, 점프는 C만,
> 카메라 `orthographicSize=3.84`, 100px=1유닛 축척 등. 되돌리지 말 것.
>
> 전체 포팅 순서와 원본 전수 분석은 `docs/ROADMAP.md` 참고(이 스프린트는 그 문서의 **1번**).

## 목표

**"맞으면 죽는다"를 "체력이 깎이고 0이 되면 죽는다"로 바꾼다.**

지금은 플레이어 공격이 적을 즉시 `Destroy()`하고, 적의 접촉 공격은 스텁(`EnemyMove.TryAttack()`이 비어 있음)이라
플레이어가 죽지 않는다. 이 스프린트가 끝나면 양쪽 모두 실제 체력을 갖고 서로 피해를 주고받는다.

이게 최우선인 이유: 보상(골드/경험치)·런 종료·엘리트/보스·원소·아이템 스탯이 **전부 체력에 의존**한다
(`docs/ROADMAP.md` 3절 의존성 분석 참고).

## 범위 — 이번에 만드는 것

1. `Core/IDamageable` — 피격 가능 대상 인터페이스
2. `Enemies/EnemyHealth` — 적 체력, 피격 플래시, 넉백, 사망
3. `Characters/PlayerHealth` — 플레이어 체력, 무적시간, 피격 반동, 사망 신호
4. 공격 스크립트를 `Destroy()` 직접 호출 → `IDamageable.TakeDamage()` 호출로 전환
5. 접촉 데미지 활성화 (`EnemyMove.TryAttack()` 스텁 채우기)
6. 치명타 판정

## 범위 밖 — 이번엔 만들지 않는다

원본에 있지만 **이번 스프린트에서 의도적으로 제외**하는 것들. 나중 스프린트에서 순서대로 붙인다.

| 제외 항목 | 이유 / 언제 |
|---|---|
| 골드·경험치 드랍, 레벨 | 스프린트 3(런 사이클 & 보상) |
| 런 종료 처리(`endRun('dead')`) | 스프린트 3. 이번엔 **사망 이벤트만 노출**하고 실제 처리는 안 함 |
| HUD 체력바 | 스프린트 3(`UI` 도메인이 그때 처음 생김). 이번 검증은 PlayMode 테스트로 |
| 레벨 페널티(`levelFactor` `:1651`) | 레벨 시스템이 없음 → 스프린트 3 이후 |
| 콤보·살기 배수(`dmgMultAll` `:1296`) | 스프린트 3 이후 |
| 아이템 관련 전부(흡혈·취약·피격무효·부활·피해감소·무적연장) | 아이템 시스템 자체가 한참 뒤 |
| 원소(화상 `burn`/출혈 `bleed`, 처형) | 나중 |
| 데미지 숫자 팝업·화면 흔들림·hitstop | UI/연출 — 방향이 미정(`docs/ROADMAP.md` 5절 4번) |
| 엘리트·보스 배수 | 적 종류 확대/보스 스프린트 |

## 확정 수치 (원본 그대로, 100px = 1유닛 축척 적용)

전부 원본에서 실제 확인한 값이다. 괄호 안은 원본 줄 번호.

### 플레이어
| 항목 | 원본 값 | Unity 값 | 원본 위치 |
|---|---|---|---|
| 최대 체력 | `baseHp: 100` | 100 | `:605` |
| 피격 무적시간 | `invulnTime: 0.9` | 0.9초 | `:606` |
| 기본 공격력 | `baseAtk: 10` | 10 | `:605` |
| 치명타 확률 | `baseCrit: 0.10` | 10% | `:605` |
| 치명타 배수 | `critMult: 1.7` | 1.7배 | `:605` |
| 피격 반동 X | `p.vx = sign(p.x - srcX) * 260` | ±2.6 유닛/s (가해자 반대 방향) | `:1908` |
| 피격 반동 Y | `p.vy = Math.min(p.vy, -220)` | **최소 +2.2 유닛/s 위로**(원본 Y+가 아래라 부호 반전) | `:1909` |
| 피격 시 접지 해제 | `p.onGround = false` | 동일 | `:1910` |

### 무기(마법사 활)
| 항목 | 원본 값 | Unity 값 | 원본 위치 |
|---|---|---|---|
| 무기 피해 배수 | `bow.dmg: 0.9` | baseAtk 10 × 0.9 = **9** | `:609` |
| 차지 피해 배수 | `dmg = base × (1 + 1.6×k)` | 이미 `MageAttack`에 구현됨 | `:610` |

> `MageAttack.baseDamage = 9`는 이미 이 값이다(스탯 시스템이 없어 `10 × 0.9`를 고정값으로 근사).
> 이번 스프린트에서 이 값이 **실제로 체력을 깎게** 연결하는 게 핵심이다.

### 적 (오니, 현재 유일한 종류)
| 항목 | 원본 값 | Unity 값 | 원본 위치 |
|---|---|---|---|
| 체력 | `oni.hp: 38` | 38 | `:709` |
| 접촉 공격력 | `oni.dmg: 13` | 13 | `:709` |
| 넉백 기본값 | `kbBase: 240` | 2.4 유닛/s | `:1678` |
| 넉백 감쇠 | `e.kbx *= max(0, 1 - 9*dt)` | 초당 9배율 지수 감쇠 | `:4021` |
| 피격 플래시 | `e.flash = 1`, `flash -= dt*6` | 1 → 0까지 약 0.167초 | `:1666`, `:4020` |

### 데미지 계산식
원본(`:1664`)에서 **이번 스프린트 범위에 해당하는 항만** 남긴 것:

```
dmg = max(1, round( atk × 무기배수 × 차지배수 × rand(0.9, 1.1) × (치명타면 1.7) ))
```

- `rand(0.9, 1.1)` — 원본에 있는 ±10% 난수 변동(`:983`, `:1664`). **빠뜨리지 말 것**
- `max(1, ...)` — 최소 피해 1 보장
- `round(...)` — 정수 피해

제외한 항(범위 밖): `dmgMultAll()`(콤보·살기·아이템·성소) · `vulnMult` · `lvF`(레벨 페널티) · `itemDmgVs` · `rebirthWallPlayerDmgMult`

### 스폰 무적
- 원본은 `dealDamage` 진입 즉시 `if (e.spawnInvuln > 0) return 0;` — **피해가 0이고 관통도 소모되지 않는다**(`:1659`)
- 이미 `ISpawnProtectable`로 구현돼 있고 공격 스크립트가 확인 중 → **`TakeDamage()` 안으로 옮겨** 한 곳에서 처리

### 접촉 데미지
- 원본은 매 프레임 겹침 검사 후 `damagePlayer(e.dmg, e.x)` 호출, **별도 쿨다운 없음**(`:4148`)
- 반복 피해 방지는 **플레이어 무적시간(0.9초)이 전담** — 적 쪽에 쿨다운을 넣지 말 것
- 조건: `player.invuln <= 0`일 때만(`:4144`)

## 구현 시 주의점

**넉백과 기존 이동 로직의 충돌** — `EnemyMove.FixedUpdate()`가 매 프레임
`rb.linearVelocity = new Vector2(vx, vy)`로 속도를 통째로 덮어쓴다. 넉백을 그냥 속도에 넣으면 다음 프레임에
즉시 지워진다. 원본은 넉백을 **별도 성분**으로 두고 AI 이동에 더한다(`e.x += (e.vx + e.kbx) * dt`, `:4056`).
같은 방식으로 `knockbackX` 필드를 따로 두고 매 프레임 감쇠시킨 뒤 `vx`에 더할 것.

**도메인 계층** — `IDamageable`은 `Core`, 구현체는 각자 도메인(`Characters`/`Enemies`).
공격 스크립트(`Combat`/`Characters`)는 `Enemies` 타입을 몰라야 하므로 반드시 `Core` 인터페이스로만 접근한다
(`ISpawnProtectable`과 같은 원리, `CLAUDE.md` asmdef 계층표 참고).

**플레이어 사망** — 런 종료는 스프린트 3 범위다. 이번엔 `PlayerHealth`가 사망 상태/이벤트만 노출하고,
받는 쪽은 만들지 않는다(빈 채로 두되 나중에 `Systems`가 구독할 수 있는 형태로).

## 검증 기준

PlayMode 테스트로 다음을 확인한다(기존 20개 + 신규):

- 적이 한 방에 안 죽는다 — 체력 38, 무차지 마법탄(9±10%)으로 여러 번 맞아야 죽음
- 체력이 0 이하가 되면 파괴된다
- 스폰 무적 중엔 피해가 0이고 관통도 소모되지 않는다
- 플레이어가 적과 접촉하면 체력이 13만큼(±10%) 깎인다
- 피격 후 0.9초 동안은 다시 안 깎인다(무적시간)
- 피격 시 가해자 반대 방향으로 밀려나고 위로 튕긴다
- 치명타가 통계적으로 발생한다(난수 고정 또는 반복 시행으로 검증)
- 넉백이 적용되고 감쇠한다

**기존 20개 테스트가 전부 통과해야 병합한다**(`CLAUDE.md` 테스트 규칙).

## 개발 순서 제안

1. `Core/IDamageable` 인터페이스
2. `Enemies/EnemyHealth` — 체력·피해·사망 (+ 넉백은 `EnemyMove`와 연동)
3. 공격 스크립트 전환(`MageProjectile`, `PlayerAttack`) — `Destroy()` → `TakeDamage()`
4. 치명타 + 데미지 난수 변동
5. `Characters/PlayerHealth` — 체력·무적시간·피격 반동
6. `EnemyMove.TryAttack()` 채우기 — 접촉 데미지
7. PlayMode 테스트 작성 → 전체 통과 확인
