# 현재 스프린트 — 단계 1: 모든 캐릭터 0차 + 모든 몬스터

> **최종 목표(변경 없음): `reference/project_test.html` 원본을 Unity로 그대로 포팅.**
> 확정 2026-09-09(회의). 완료·보류된 이전 스펙은 `docs/sprints/`에 있다(현재 스펙 아님).
>
> | 문서 | 소유하는 것 |
> |---|---|
> | **이 파일** | 지금 스프린트의 범위·수치·완료 기준 |
> | `docs/worksplit.md` | 분업(나 = 뼈대·메카닉·몹 전부 / 팀원 = 섬영·드루이드) |
> | `docs/original-parity.md` | 원본↔Unity 전수 대조표 + 원본 위치 색인 |
> | `PROGRESS.md` | 지금 상태·다음 할 일·작업 재개 방법 |

## 배경 — 회의에서 정한 진행 방식

```
단계 1  모든 캐릭터를 "0차"까지 + 모든 몬스터를 프로토타입 그대로   ← 이번 스프린트
단계 2  캐릭터 하나씩 깊게 5차까지
단계 3  완성된 캐릭터부터 하나씩 밸런스 조정 (기획자 상윤)
```

밸런스를 잡으려면 **비교 대상이 다 있어야** 한다 — 캐릭터 하나만 깊게 파 놓고 다른 캐릭터·몹이 없으면
"이 캐릭터가 센 건지 몹이 약한 건지"를 못 가른다. 그래서 폭을 먼저 채우고 깊이를 나중에 판다.

## "0차"의 정확한 정의 — **기본공격만 있는 상태**

전문화(스킬트리)를 **아직 안 찍은 상태**다. 원본은 `tier < 1`이면 X 스킬을 막고 메시지만 띄운다
(`tryMageSkill` `:2169` · `tryBladeSkill` `:2741` · `tryDruidSkill` `:3385`).

- **넣는 것**: 그 캐릭터의 **Z 기본공격** + 그 캐릭터가 굴러가는 데 필요한 **이동 방식**
- **빼는 것**: X 스킬 전부, 티어별 보너스 전부

> 기본공격 코드가 자원을 채우는 부분(메카닉 적중 시 스택 +1, 드루이드 적중 시 마나 +8%)은
> **기본공격의 일부라 같이 이식하되, 0차에선 쓸 곳이 없어 쌓이기만 한다** — 소비처가 전부 X 스킬이라
> 티어가 있어야 열린다. 원본도 동일하다.

> **마법사는 이미 5차까지 끝나 있다**(폭발·중력 두 갈래). **0차로 되돌리지 않는다** —
> 단계 2의 "첫 번째로 깊게 판 캐릭터"가 이미 마법사인 셈이다.

## 이번 스프린트에서 만드는 것

### 1. 플레이어 뼈대 (착수 전제 — 팀원이 기다리고 있다, 이것부터)
- `Core/CharacterId` — 캐릭터 4종 + **캐릭터별 스탯 배수**(원본 `CHAR_STAT_MULT` `:1282`, 섬영만 1.5.
  `statAtk`·`statMaxHp`에만 곱하고 ms/as/crit엔 안 곱한다 — `:1284`~`:1288` 확인)
- `Characters/CharacterMover2D`에 **관성 이동 모드**(원본 `bladeMove` `:2454`) — 섬영이 쓴다
- `Characters/PlayerRig` + `ICharacterKit` — 선택된 캐릭터의 키트만 켠다(원본 `updatePlayer`의 무기별 분기 `:3509`)
- 캐릭터 전환용 임시 디버그 키

정확한 시그니처는 `docs/worksplit.md` 3절 "공유 계약" — **거기 적힌 이름·타입을 바꾸지 말 것.**

### 2. 메카닉 0차 (`gunnerFire` `:2188`, laserTier=0 분기)
| 항목 | 값 | 비고 |
|---|---|---|
| 쿨다운 | `0.24 / statAs()` | 원본 `:2196` |
| 총알 속도 | 13.2 유닛/s | 원본 1320 ÷100 |
| 수명 | 0.62초 | |
| 피해 배수 | 0.82 | `statAtk × 0.82` |
| 관통 | 1 | |
| 적중 시 | 스택 +1 (`stackOnHit`) | 스택 소비처는 티어라 0차엔 쌓이기만 |
| 공격 모션 | `0.11 / statAs()` | `attackRapid` |

### 3. `EnemyData` ScriptableObject 전환 ⚠️ 몹 추가보다 **먼저**
CLAUDE.md 규칙: **"2번째 적 종류를 만들기 전에 SO 전환을 끝내고 병합한다."**
지금 `EnemyMove`/`EnemyHealth`에 하드코딩된 오니 수치를 `EnemyData`(SO)로 옮기고, 7종 수치를
`CONFIG.enemyBase`(`:707`~`:714`) 그대로 에셋으로 만든다.

### 4. 몹 6종 추가 (원본 `CONFIG.enemyBase` `:707`~`:714`)
| 몹 | hp/dmg/speed | 행동 | 원본 |
|---|---|---|---|
| wisp 도깨비불 | 26/8/66 | **비행** — 지면 무시, 플레이어 Y를 수직 속도 46으로 추적 | `:700`, `:3956` |
| bigOni 대오니 | 160/26/44 | 오니와 같은 AI, **넉백 저항 0.4** | `:710`, `:1678` |
| charger 돌진귀 | 50/15/58 | 조준 0.5초 → 돌진(속도 500) → 탈진 0.9초, 쿨 2.6초, 돌진 피해 ×1.4 | `:716` |
| shooter 사수귀 | 30/10/55 | 거리 300~500 유지 + 투사체(속도 380, 수명 3초), 쿨 2.4초 | `:717` |
| splitter 분열귀 | 55/12/60 | **죽으면 새끼 2마리**(좌우 ±0.24유닛, 스폰보호 0.35초) | `:1840` |
| splitlet 새끼 | 14/7/112 | 빠른 소형 | `:714` |

⚠️ **splitter의 "죽을 때 새끼 스폰"은 `Enemies`가 `Systems`(스포너)를 직접 부르면 순환참조로 컴파일이 깨진다.**
`Core` 이벤트 버스로 요청을 넘길 것 — CLAUDE.md "낮은 층이 높은 층의 기능을 요청" 항목에 패턴이 있다.

### 5. 엘리트 (`:696`, `:3949`)
8% 확률, 체력 ×4 · 피해 ×1.5 · 보상 ×5 · 크기 ×1.35. 모든 타입에 붙는 수식어다.

### 6. 지역별 스폰 테이블 (`rollSpawnType` `:3933`)
지역이 오를수록 강한 종이 확률표에 추가된다. 몹 레벨 `enemyLv()`(`:3924`)도 같이.

## 팀원 몫 (병렬)
- **A. 섬영 0차** — 관성 이동, 회전베기 Z, 속도→피해, 흡혈 15%, 방향키 피격무적
- **B. 드루이드 0차** — 클로 3타 콤보 Z, 마나(최대 20, 평타 적중 시 8% 회복)

수치·원본 줄은 `docs/worksplit.md` 4절 표 참고.

## 완료 기준 (DoD)

- [ ] 디버그 키로 캐릭터 4종을 전환하며 각자의 이동·Z 평타가 **원본대로** 동작한다
- [ ] 섬영은 달릴수록 세지고 방향 전환이 감속 없이 즉시 뒤집힌다
- [ ] 몹 7종이 각자 원본 AI대로 움직이고, 엘리트가 8%로 섞여 나온다
- [ ] 분열귀가 죽을 때 새끼 2마리가 나온다(순환참조 없이)
- [ ] 몹 수치가 전부 `EnemyData` SO에 있고 코드에 하드코딩이 없다
- [ ] 종류별 PlayMode 테스트 + 전체 통과 후 `main` 병합

## 범위 밖 (이번엔 안 만든다)

- **X 스킬·전문화 티어 전부**(마법사 제외 — 이미 완료). 단계 2에서.
- 런 사이클·로비·HUD·세이브 — 스펙은 `docs/sprints/04-run-cycle.md`에 보존, 단계 3 직전에 실행
- 성소·보스·지역 진행/입장료·경험치 구슬·전투 배수 체인·아이템/가챠/윤회
- 캐릭터별 레벨/SP 분리(원본 `charProgress` `:1249`) — 단계 2에서 `branch`/`tier` 일반화와 같이
- 렌더링·비주얼·사운드·연출 전부
