# 현재 스프린트 — 단계 1: 모든 캐릭터 0차 + 모든 몬스터

> **최종 목표(변경 없음): `reference/project_test.html` 원본을 Unity로 그대로 포팅.**
> 확정 2026-09-09(회의). 완료·보류된 이전 스펙은 `docs/sprints/`에 있다(현재 스펙 아님).
>
> | 문서 | 소유하는 것 |
> |---|---|
> | **이 파일** | 지금 스프린트의 범위·수치·완료 기준 |
> | `docs/worksplit.md` | 분업(나 = 뼈대·메카닉·몹·시스템 전부 / 팀원 = 섬영·드루이드) + **공유 계약** |
> | `docs/original-parity.md` | 원본↔Unity 전수 대조표 + 원본 위치 색인 |
> | `PROGRESS.md` | 지금 상태·다음 할 일·작업 재개 방법 |

## 남은 것은 **섬영 0차 · 드루이드 0차뿐이다** (팀원)

단계 1에서 내 몫은 전부 끝났다. 키트를 붙이는 사람은 아래 **"⚠️ 키트 작성자가 알아야 할 것"**과
`docs/worksplit.md` 3절(공유 계약)만 보면 착수할 수 있다.

## 배경 — 회의에서 정한 진행 방식

```
단계 1  모든 캐릭터를 "0차"까지 + 모든 몬스터를 프로토타입 그대로   ← 이번 스프린트
단계 2  캐릭터 하나씩 깊게 5차까지 (메카닉 스킬트리부터)
단계 3  완성된 캐릭터부터 하나씩 밸런스 조정 (기획자 상윤)
```

밸런스를 잡으려면 **비교 대상이 다 있어야** 한다 — 캐릭터 하나만 깊게 파 놓고 다른 캐릭터·몹이 없으면
"이 캐릭터가 센 건지 몹이 약한 건지"를 못 가른다. 그래서 폭을 먼저 채우고 깊이를 나중에 판다.

## "0차"의 정확한 정의 — **기본공격만 있는 상태**

전문화(스킬트리)를 **아직 안 찍은 상태**다. 원본은 `tier < 1`이면 X 스킬을 막고 메시지만 띄운다
(`tryMageSkill` `:2169` · `tryBladeSkill` `:2741` · `tryDruidSkill` `:3385`).

- **넣는 것**: 그 캐릭터의 **Z 기본공격** + 그 캐릭터가 굴러가는 데 필요한 **이동 방식**
- **빼는 것**: X 스킬 전부, 티어별 보너스 전부

> 기본공격 코드가 자원을 채우는 부분(메카닉 적중 시 스택 +1, 드루이드 적중 시 마나 +8%)은
> **기본공격의 일부라 같이 이식하되, 0차에선 쓸 곳이 없어 쌓이기만 한다** — 소비처가 전부 X 스킬이라
> 티어가 있어야 열린다. 원본도 동일하다.

> **마법사는 이미 5차까지 끝나 있다**(폭발·중력 두 갈래). **0차로 되돌리지 않는다** —
> 단계 2의 "첫 번째로 깊게 판 캐릭터"가 이미 마법사인 셈이다.

---

## 팀원 몫 (단계 1의 마지막 조각)

- **A. 섬영 0차** — 관성 이동, 회전베기 Z, 속도→피해, 흡혈 15%, 방향키 피격무적
- **B. 드루이드 0차** — 클로 3타 콤보 Z, 마나(최대 20, 평타 적중 시 8% 회복)

수치·원본 줄은 `docs/worksplit.md` 4절 표. **공유 계약(이름·시그니처)은 같은 문서 3절** — 거기 적힌
이름을 바꾸지 말 것.

### ⚠️ 키트 작성자가 알아야 할 것 (전부 "에러가 안 나는" 함정이다)

1. **런 중 생성물에 `Core/RunTransient`를 반드시 붙일 것** — 참격 판정체, 늑대, 투사체, 장판 전부.
   안 붙이면 로비로 나갔다 와도 필드에 남는다. `RunTransient.Mark(go)` 한 줄이다.
2. **`Instantiate`/`AddComponent` 직후에 필드만 바꾸면 이미 끝난 `Awake()`에는 반영이 안 된다.**
   적 체력은 반드시 `EnemyHealth.SetMaxHp()`로. 이 함정으로 이미 세 번 깨졌다.
3. **공속·피해 배수는 신경 쓸 게 없다.** 살기(fury) 공속은 원본과 같이 `statAs()` 안에 들어 있어
   (`PlayerStatCalculator.ComputeAttackSpeedMultiplier`), 쿨다운을 `기본값 / statAs`로 계산하면 자동으로 붙는다.
   콤보·레벨페널티도 `DamageCalculator`가 처리한다.
4. **쿨다운에는 `PlayerStatCalculator.ComputeCooldownMultiplier`도 곱할 것** — 아이템 '시간의 조각'이다.
   원본은 기본공격·궁극기 쿨타임 **전부**에 곱한다(`:1942`·`:2177`·`:2196`).
5. **`Time.timeScale = 0`을 전제로 짜지 말 것** — 반대로, 그 덕에 `IsRunning` 검사가 아예 필요 없다.
   로비·일시정지에선 `Time.deltaTime`이 0이라 쿨다운도 안 돈다. 멈춰도 돌아야 하는 것만 `unscaledDeltaTime`.
6. **정의만 하고 호출부를 안 만드는 실수를 조심할 것.** 2026-09-10 감사에서 나온 편차 10건 중 절반이
   이 모양이었다(아래 8번). 정의와 사용처가 다른 파일인 공개 API를 만들면
   `OriginalFidelityTests.DefinedApis_AreActuallyCalledSomewhere`의 목록에 추가할 것.
7. **덩치는 `Core/EntitySizeConfig`에서 온다** — 캐릭터 그림을 붙일 때 `PlayerRig.spriteHeight`를
   직접 만지지 말 것. 콜라이더가 자동으로 따라간다.

---

## 완료된 것 (전부 `main` 병합·검증 완료)

### 1. ✅ 플레이어 뼈대
`Core/CharacterId`(+캐릭터 스탯배수, 원본 `CHAR_STAT_MULT` `:1282` — 섬영만 1.5, `statAtk`·`statMaxHp`에만),
`CharacterMover2D` 관성 이동 모드(원본 `bladeMove` `:2454`), `PlayerRig` + `ICharacterKit`.

### 2. ✅ 메카닉 0차 (`gunnerFire` `:2188`, laserTier=0 분기)
| 항목 | 값 | 비고 |
|---|---|---|
| 쿨다운 | `0.24 / statAs() * cdMult()` | 원본 `:2196` |
| 총알 속도 | 13.2 유닛/s | 원본 1320 ÷100 |
| 수명 | 0.62초 | |
| 피해 배수 | 0.82 | `statAtk × 0.82` |
| 관통 | 1 | |
| 적중 시 | 스택 +1 (`stackOnHit`) | 스택 소비처는 티어라 0차엔 쌓이기만 |

### 3. ✅ `EnemyData` ScriptableObject 전환
몹 수치는 전부 `Assets/Data/Enemies/`에 있다. 코드에 하드코딩 없음.

### 4. ✅ 몹 7종 (원본 `CONFIG.enemyBase` `:707`~`:714`)
| 몹 | hp/dmg/speed | 행동 | 원본 |
|---|---|---|---|
| wisp 도깨비불 | 26/8/66 | **비행** — 지면 무시, 플레이어 Y를 수직 속도 46으로 추적 | `:700`, `:3956` |
| oni 오니 | 38/13/76 | 기본 보행 | `:709` |
| bigOni 대오니 | 160/26/44 | 오니와 같은 AI, **넉백 저항 0.4** | `:710`, `:1678` |
| charger 돌진귀 | 50/15/58 | 조준 0.5초 → 돌진(속도 500) → 탈진 0.9초, 쿨 2.6초, 돌진 피해 ×1.4 | `:716` |
| shooter 사수귀 | 30/10/55 | 거리 300~500 유지 + 투사체(속도 380, 수명 3초), 쿨 2.4초 | `:717` |
| splitter 분열귀 | 55/12/60 | **죽으면 새끼 2마리**(좌우 ±0.24유닛, 스폰보호 0.35초) | `:1840` |
| splitlet 새끼 | 14/7/112 | 빠른 소형. **스폰 표엔 절대 안 들어간다** | `:714` |

**엘리트**(`:696`, `:3949`) 8% — 체력 ×4 · 피해 ×1.5 · 보상 ×5 · 크기 ×1.35. 모든 타입에 붙는 수식어.
**지역별 스폰 표**(`rollSpawnType` `:3933`) — `Systems/EnemySpawnTable`.

> **몹을 만질 때 걸리기 쉬운 세 가지**
> 1. 스포너는 `Instantiate` **뒤에** 컴포넌트를 붙이므로 `EnemyMove.RefreshTypeBehaviour()`를 반드시 부른다.
>    안 부르면 도깨비불이 중력에 떨어지는 **에러 없이 조용히 틀리는** 상태가 된다.
> 2. 몸집은 콜라이더 반지름이 아니라 `transform.localScale`로 준다(그래야 그림과 판정이 같이 커진다).
>    실제 반지름은 `EnemyMove.WorldRadius`로 읽을 것. 기준값은 `Core/EntitySizeConfig`.
> 3. 돌진귀 상태 전이는 원본이 속도를 정한 **뒤에** 상태를 바꾼다(`:4065`~`:4082`). 전이를 먼저 하면
>    질주 마지막 프레임이 0이 되어 원본과 달리 멈칫한다. 테스트로 고정해뒀다.

### 5. ✅ 런 사이클 + 로비 7탭 (`9d5dd80`·`808e323`)
`Systems/RunController`(로비↔사냥↔일시정지↔결과) · `Core/RunTransient` · `Assets/Scripts/UI/`.
탭은 **캐릭터 선택 / 스테이지 / 강화 / 전문화 / 윤회 / 기원 / 업적** 7개.

**일시정지는 `Time.timeScale = 0`**(확정 설계). 원본은 한 개의 `frame()` 루프라 `dt=0`으로 한 번에
멈추는데(`:7169`) 우리는 컴포넌트마다 Update가 따로 돈다. 이 방식 덕에 적·스폰너·투사체에
`IsRunning` 가드가 하나도 필요 없다.

**임시방편 3종은 삭제됐다** — R키 부활, Tab 캐릭터 전환, 숫자키 강화·6/7 빌드 선택. 전부 원본에 없다.

### 6. ✅ 전투 배수 체인 + 세이브 (`6d96e51`·`ffabf52`)
`Core/CombatModifiers` — 콤보(3초 창, 피해 +1.2%/콤보) · 살기(처치당 +0.8%, 런 내내 누적) ·
연쇄 처치(0.8초 안 3킬부터 골드 보너스) · 레벨 페널티(레벨당 -5%, 하한 25% / **보스는 50%**) · 성소 버프.
히트스톱은 `RunController.Hitstop()` — `timeScale`을 만지는 유일한 주체라서.
**지속 피해 틱엔 안 걸린다**(원본 `!opts.isBurnTick`) — `IDamageable.TakeTickDamage()`로 갈랐다.

`Systems/SaveService` + `AutoSave` — JSON. 기본값 위에 덮어쓰므로 나중에 필드가 늘어도 옛 세이브가 열린다.

### 7. ✅ 성장 고리 완성
**사냥 → 지역 100마리 토벌 → 보스 → 다음 지역 → (지수 입장료 벽) → 윤회 → 뽑기 → 아이템 → 더 깊은 지역.**

경험치 구슬(`c195339`) · 성소(`9b16902`) · 지역 진행도+보스(`3ac149e`) ·
프로토타입 그림 14장(`74247f2`, 절차 `docs/prototype-sprites.md`) · 아래키 낙하 + 보스 무대(`a1d4422`) ·
윤회 + 윤회 장벽(`8fa3390`) · 아이템 35종 + 가챠(`cfa1bd5`) · 업적 32종(`030b784`).

### 8. ✅ 원본 편차 10건 수정 + UI 가독성 (2026-09-10, `cc410d1`·`3a725dd`)

원본 대조 감사에서 나온 편차를 전부 고쳤다. **절반이 같은 모양이었다 — 값은 정의해 놓고 호출부를
안 만든 것.** 컴파일러도 기존 테스트도 아무 말 안 해서 조용히 원본과 다르게 돌아갔다.

- 보스 레벨 페널티 하한(0.5) 미적용 · 쿨감 아이템 미연결 · 구슬 경험치 배수 누락
- 웨이브 상한이 보스·성소까지 셌다(원본 `:3978`은 둘을 제외한다)
- 보스 부하가 분열귀 새끼용 0.35초 보호를 받았다 → `EnemySpawnRequestBus.Request`에 보호 시간을 인자로
- 성소 4건: 스폰 보호 미적용 / 보스전에도 등장 / 보호 중에도 적 버프 / 살아 있으면 타이머 정지
- 구운 그림 3장(boss·shrine·exp_orb)이 놀고 있었다

**재발 방지**: `Tests/PlayMode/OriginalFidelityTests` — 편차별 회귀 테스트 +
`DefinedApis_AreActuallyCalledSomewhere`(소스를 직접 훑어 호출부 없는 공개 API를 잡는다).

**UI 가독성**(`UI/UiTheme`) — 로비·결과·HUD가 전부 사냥터 위에 그대로 얹혀 안 읽혔다. 원본이 자리마다
다르게 푸는 방식을 그대로 옮겼다: 읽어야 할 덩어리는 판으로 덮고(로비 본문 `rgba(14,11,20,.92)`,
결과 화면 전체 `rgba(5,4,10,.88)`), 덮을 수 없는 자리(헤더·HUD)는 글자에 그림자를 준다.
HUD 수치는 바로 보여준다 — 바 자체가 작은 대비 영역이 된다.

### 9. ✅ 덩치 정리 (2026-09-10)

사용자 지시: **"성소가 너무 작다 / 몬스터는 반으로 / 플레이어는 1.5배"**.
값이 세 군데에 흩어져 있어서(몹은 `EnemyData`, 플레이어는 `PlayerRig`, 성소·보스는 스포너에 박힌
`localScale`) 어긋나도 알기 어려웠다 → **`Core/EntitySizeConfig` 한 곳으로 모았다.**

| | 원본(유닛) | 지금 |
|---|---|---|
| 플레이어 | 0.34×0.56 | 높이 **1.50** ← 사용자 지정(원본의 2.7배) |
| 오니 | 0.42×0.46 | 반지름 0.25(= 높이 0.50) — 거의 원본 |
| 성소 | 0.64×0.96 | **원본 그대로** |
| 보스 | 1.30×1.50 | **원본 그대로** |

> 성소가 작아 보인 원인: 프리팹 기본 스프라이트가 32×32짜리 내장 이미지라, 거기 맞춰 손으로 고른
> `localScale`(1.3×1.9)이 실제로는 0.42×0.61유닛 — **오니보다도 작았다.** 손으로 고른 숫자를 없애고
> 기준을 "원본 유닛 크기"로 바꿨다.
>
> ⚠️ **플레이어만 원본보다 크다.** 원본 비율은 플레이어가 오니보다 살짝 큰 정도(1.22배)인데
> 지금은 3배다. 밸런스를 볼 때 되돌리려면 `EntitySizeConfig.PlayerHeight` 하나만 만지면 된다.

---

## 완료 기준 (DoD)

- [x] 몹 7종이 각자 원본 AI대로 움직이고, 엘리트가 8%로 섞여 나온다
- [x] 분열귀가 죽을 때 새끼 2마리가 나온다(순환참조 없이)
- [x] 몹 수치가 전부 `EnemyData` SO에 있고 코드에 하드코딩이 없다
- [x] 로비에서 캐릭터를 고르고 강화·전문화를 찍은 뒤 런에 들어간다 (`808e323`)
- [x] 전투 배수 체인 + 세이브/로드 (`6d96e51`·`ffabf52`)
- [x] 윤회·뽑기·업적 (`8fa3390`·`cfa1bd5`·`030b784`)
- [x] 경험치 구슬 · 성소 · 지역 진행도 + 보스 (`c195339`·`9b16902`·`3ac149e`)
- [x] 프로토타입 그림 추출·적용 (`74247f2`) · 아래키 낙하 + 보스 무대 (`a1d4422`)
- [x] 원본 편차 10건 수정 + UI 가독성 + 덩치 정리 (`cc410d1`·`3a725dd`)
- [ ] **로비에서 캐릭터 4종을 골라 각자의 이동·Z 평타가 원본대로 동작한다** *(마법사·메카닉 ✅ / 섬영·드루이드 팀원 진행 중)*
- [ ] **섬영은 달릴수록 세지고 방향 전환이 감속 없이 즉시 뒤집힌다** *(팀원)*

## 범위 밖 (이번 단계에서 안 만든다)

- **X 스킬·전문화 티어 전부**(마법사 제외 — 이미 완료). 단계 2에서, **메카닉 스킬트리가 첫 순서**다.
- **캐릭터별 레벨/SP 분리**(원본 `charProgress` `:1249`) — 단계 2에서 `branch`/`tier` 일반화와 같이.
- 사운드·파티클·화면 흔들림·데미지 숫자 팝업.
- 원본 렌더링 코드(`:4482`~`:6215`) 자체의 이식. **그림은 이미 PNG로 구워 쓰고 있다.**

### 아직 안 붙은 것 (범위 밖이 아니라 미완)

- **조건부 특수 아이템 11종** — `ambush`·`giantSlayer`·`crowd`·`execute`·`lastStand`·`airDmg`·
  `killSpeed`·`killRage`·`plague`·`deathBlast`·`lifesteal`. 전부 **피격 순간의 대상/플레이어 상태**
  (적 체력 비율, 주변 적 수, 공중 여부)를 알아야 하는데 지금 피해 경로에 그 정보가 안 흐른다.
  정의는 다 있고 가챠에서도 나온다 — 피해 계산에 대상 컨텍스트를 넘기는 작업과 함께 붙이면 된다.
- **보스 체력바 · 스킬 쿨다운 슬롯** — 원본 `#bossbar`(`:49`) · `.slot`(`:60`). HUD에 자리만 없다.
- **연쇄 처치 슬로모** — 원본 연출.
